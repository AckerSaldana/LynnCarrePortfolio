---
interface Segment {
  t: string;
  em?: 'steel' | 'bloom';
}

interface Props {
  name: string;
  location: string;
  year: string;
  manifesto: Segment[];
}

const { name, location, year, manifesto } = Astro.props;
---

<section data-frame data-tag="índice" data-steps="0" class="ix">
  <div class="ix__inner">
    <h2 class="ix__manifesto">{manifesto.map((s) =>
      s.em === 'bloom' ? <em class="ix__em ix__em--bloom">{s.t}</em>
      : s.em === 'steel' ? <em class="ix__em">{s.t}</em>
      : s.t
    )}</h2>

    <div class="ix__meta">
      <div class="ix__meta-group">
        <span class="ix__meta-k">Diseñadora</span>
        <span class="ix__meta-v">{name}</span>
      </div>
      <div class="ix__meta-group ix__meta-group--right">
        <span class="ix__meta-k">Portafolio · {year}</span>
        <span class="ix__meta-v">{location}</span>
      </div>
    </div>
  </div>
</section>

<style>
  .ix {
    position: absolute;
    inset: 0;
    background: var(--paper);
    color: var(--ink);
  }
  .ix__inner {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--frame-pad);
    box-sizing: border-box;
  }

  /* la declaración: protagonista centrada, acotada por ancho Y alto */
  .ix__manifesto {
    margin: 0;
    max-width: 22ch;
    text-align: center;
    font-family: var(--font-display);
    font-weight: 400;
    line-height: 1.04;
    letter-spacing: -0.02em;
    font-size: clamp(2.6rem, min(8.5vw, 12vh), 9.5rem);
  }
  .ix__em { font-style: italic; }
  .ix__em--bloom {
    font-style: italic;
    color: var(--blue);
  }

  /* máscara por línea (creada en runtime por SplitText): respiro lateral para que la
     cursiva no se recorte; el negativo del margen mantiene la alineación a la izquierda */
  .ix__manifesto :global(.ix__line-mask) {
    display: block;
    overflow: hidden;
    padding: 0.06em 0.24em;
    margin: 0 -0.24em;
  }
  .ix__manifesto :global(.ix__line) { display: block; }

  /* pie editorial: footer fijo al fondo del frame */
  .ix__meta {
    position: absolute;
    left: var(--frame-pad);
    right: var(--frame-pad);
    bottom: clamp(2.4rem, 6vh, 4rem);
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    gap: 2rem;
  }
  .ix__meta-group {
    display: flex;
    flex-direction: column;
    gap: 0.45rem;
  }
  .ix__meta-group--right { text-align: right; }
  .ix__meta-k {
    font-family: var(--font-body);
    font-size: 0.64rem;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    opacity: 0.5;
  }
  .ix__meta-v {
    font-family: var(--font-body);
    font-size: 0.92rem;
    font-weight: 500;
  }

  @media (max-width: 820px) {
    .ix__manifesto { max-width: 18ch; }
    .ix__meta {
      flex-direction: column;
      align-items: flex-start;
      gap: 1.2rem;
    }
    .ix__meta-group--right { text-align: left; }
  }
</style>
